[build]
builder = "nixpacks"

[build.config]
nixPkgs = ["python3", "nodejs", "yarn"]

[build.phase]
build = "cd frontend && yarn install && yarn build && cd ../backend && pip install -r requirements.txt"

[start]
cmd = "cd backend && uvicorn server:app --host 0.0.0.0 --port $PORT"
